## Deprecation policy

This repo exposes one frozen contract line: package `blux-coga` 1.0.0 with
engine identity `CogA-1.0-pro`.

There are no active deprecations in this release.

The legacy top-level `run` alias and legacy header backfill remain supported as
part of the documented compatibility surface, not as pending removals.
