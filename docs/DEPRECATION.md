## Deprecation policy

This repository exposes one frozen contract line: package `blux-coga` 1.0.0
with engine identity `CogA-1.0-pro`.

There are no active deprecations in this release.

The documented CLI aliases and run-header backfill behavior are part of the
current compatibility surface, not pending removals.
