## PRO notes

Engine identity `CogA-1.0-pro` is the frozen runtime label recorded in
`run_header.model_version`.

In this repository state, the `-pro` label does not introduce a separate
contract, alternate schema, or alternate file output path. The package release,
contract version, determinism rules, and canonical filenames remain those
documented elsewhere in this repo.
